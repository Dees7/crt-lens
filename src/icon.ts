/**
 * Иконка пункта меню — окно терминала с приглашением `>_`.
 *
 * Цвет — `currentColor`: Lens красит вложенные элементы правилом
 * `.Icon.svg svg * { fill: currentColor }`, а своего цвета у `.Icon` нет,
 * поэтому иконка берёт цвет соседних пунктов меню в любой теме и
 * подсвечивается на hover вместе с ними.
 *
 * Сетка 24x24 под вес material-иконок: рамка 2px, внутри курсор и черта
 * ввода. Заменить на обычную material-иконку можно через material_icon
 * в конфиге.
 */
/**
 * График с осями — иконка для кнопки логов.
 *
 * В material-наборе ничего похожего нет, а `insights` обычно уже занят кнопкой
 * мониторинга, и рядом их было не различить.
 *
 * Геометрия снята с растра 32x32, поэтому viewBox тоже 32x32:
 *   ось Y   — x 0..2,  y 4..24
 *   ось X   — x 0..31, y 26..28
 *   график  — от (4,8) вниз в ложбину (11,14), пик (17.5,4), ложбина (24.5,14),
 *             вверх к правому краю (32,7.5), заливка до y=25
 *
 * Цвет — везде currentColor, чтобы иконка жила в теме приложения и
 * подсвечивалась на hover как соседние пункты.
 */
export const CHART_ICON_SVG = [
  '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="currentColor">',
  '<rect x="0" y="4" width="3" height="21" fill="currentColor"/>',
  '<rect x="0" y="26" width="32" height="3" fill="currentColor"/>',
  '<path d="M4 8 L11 14 L17.5 4 L24.5 14 L32 7.5 L32 25 L4 25 Z" fill="currentColor"/>',
  "</svg>",
].join("");

export const TERMINAL_ICON_SVG = [
  '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">',
  // рамка окна
  '<path d="M3 4h18a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1zm1 2v12h16V6H4z"/>',
  // курсор-стрелка
  '<path d="M6.6 8.6 10.4 12l-3.8 3.4-1.3-1.5L7.7 12 5.3 10.1z"/>',
  // черта ввода
  '<rect x="11.5" y="14.2" width="6.5" height="1.8"/>',
  "</svg>",
].join("");

/**
 * Встроенные иконки под именами: `icon: 'svg:chart'` в конфиге.
 *
 * Нужны там, где в material-наборе подходящего значка нет, а вставлять в YAML
 * простыню XML не хочется. Имя, которого здесь нет, уходит в `svg` как есть —
 * то есть как имя иконки самого приложения или как XML.
 */
export const BUILTIN_ICONS: Record<string, string> = {
  terminal: TERMINAL_ICON_SVG,
  chart: CHART_ICON_SVG,
};
